{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "xmake & xmake d",
            "group": {
                // 从这里
                "kind": "build",
                "isDefault": true
              }, // 到这里，设置为默认构建任务，按Ctrl+Shift+B立即执行，不必选择
        }
    ]
}